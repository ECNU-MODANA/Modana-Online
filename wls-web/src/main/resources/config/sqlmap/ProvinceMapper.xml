<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wls.manage.dao.ProvinceMapper">

    <select id="findProvinceList" parameterType="map" resultType="Province_infoEntity">
        SELECT *
        FROM `province_info` 
    </select>

</mapper>